{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm -r build && shx cp index.html ./api/index.html",
    "clean": "shx rm -rf ./dist",
    "start": "node ./dist/api/server.js",
    "dev": "ts-node ./packages/backend/src/server.ts"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0",
    "shx": "^0.4.0"
  },
  "dependencies": {
    "@types/express": "^5.0.5",
    "@vercel/node": "^5.5.6",
    "express": "^5.1.0"
  }
}
